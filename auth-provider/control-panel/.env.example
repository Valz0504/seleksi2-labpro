# URL reached by the user's browser, not the Docker service hostname.
AUTH_SERVER_PUBLIC_URL=http://localhost:3001

# URL reached by the Control Panel server process.
AUTH_SERVER_INTERNAL_URL=http://localhost:3001
